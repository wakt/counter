<%= form_for(@taxdetail) do |f| %>
  <% if @taxdetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taxdetail.errors.count, "error") %> prohibited this taxdetail from being saved:</h2>

      <ul>
      <% @taxdetail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
